# 任务书：V10.0 构建系统重构 — 全TXT驱动世界书

> 创建日期：2026-06-28
> 前置：`00_任务书_V9脚本与项目文档更新.md`（已完成，归档）
> 目标：将 build_eldoria.py 从"硬编码条目 + 事件TXT"重构为"全部条目由docs/子目录TXT驱动"

---

## 一、背景

### 1.1 当前状态

V9.0完成了脚本层更新和事件系统规范化。但 `build_eldoria.py` 存在严重遗留问题：

| 条目来源 | 数量 | 方式 | 问题 |
|----------|:--:|------|------|
| 步骤1 (uid 0-30) | 27条 | 硬编码 | 与 `docs/character/` `docs/world/` 等TXT重复/不一致 |
| 步骤2a-2i (uid 31-109) | 56条 | 硬编码 | 同上，包含已废弃的旧路线/旧变量内容 |
| 步骤2ch0-2ch8 | 170条 | TXT驱动 ✅ | 事件条目，已正常工作 |
| 其他 | 9条 | 硬编码 | — |
| **总计** | **262条** | 83条硬编码 + 170条TXT + 9条其他 | — |

**核心问题**：docs/下有69个精心维护的TXT文件（chapter/ 6 + character/ 14 + world/ 14 + creature/ 11 + location/ 20 + npc/ 4），但构建脚本完全不读取它们。硬编码内容与TXT内容重复且不一致。

### 1.2 目标架构

```
docs/
├── chapter/     (6 TXT, 始终触发)  ──→ 世界书条目 (constant=true)
├── character/   (14 TXT + 1 总览)  ──→ 世界书条目 (总览constant=true, 其他由key触发)
├── world/       (14 TXT + 1 总览)  ──→ 世界书条目 (同上)
├── creature/    (11 TXT + 1 总览)  ──→ 世界书条目 (同上)
├── location/    (20 TXT + 1 总览)  ──→ 世界书条目 (同上)
├── npc/         (4 TXT + 1 总览)   ──→ 世界书条目 (同上)
└── event/{章节}/ (170 TXT)         ──→ 世界书条目 (事件，非始终触发)

build_eldoria.py = 全TXT驱动，零硬编码内容
```

### 1.3 参考格式

参照 `reference/我的妹妹不可能这么可爱京介版本（ver1.41）.json` 的条目格式：

```json
{
    "uid": 0,
    "key": ["关键词1", "关键词2"],
    "keysecondary": [],
    "comment": "条目名称",
    "content": "条目正文内容...",
    "constant": true,
    "vectorized": false,
    "selective": true,
    "selectiveLogic": 0,
    "addMemo": true,
    "order": 100,
    "position": 0,
    "disable": false,
    "excludeRecursion": false,
    "preventRecursion": false,
    "delayUntilRecursion": false,
    "probability": 100,
    "useProbability": true,
    "depth": 4,
    "group": "",
    "groupOverride": false,
    "groupWeight": 100,
    "scanDepth": null,
    "caseSensitive": null,
    "matchWholeWords": null,
    "useGroupScoring": false,
    "automationId": "",
    "role": null,
    "sticky": 0,
    "cooldown": 0,
    "delay": 0,
    "displayIndex": 0
}
```

核心字段映射：
| TXT字段 | JSON字段 | 说明 |
|----------|----------|------|
| `名称` | `comment` | 条目名称 |
| `触发关键词` | `key` | 逗号分隔→数组 |
| `始终触发` | `constant` | "是"→true, "否"→false |
| `注入深度` | `depth` | 数字 |
| `内容` | `content` | 条目正文（bullet list合成段落） |
| — | `order` | 从100开始递增 |
| — | `selective` | constant=false时true，constant=true时true |

---

## 二、现状审计

### 2.1 Docs子目录TXT文件统计

| 目录 | 文件数 | 始终触发 | 非始终触发 | TXT格式 |
|------|:--:|:--:|:--:|:--:|
| `chapter/` | 6 | 6 (`_阶段系统`, `_隐奸与NTRS概念`, `_好感度分级系统`, `_游戏状态界面`, `_事件追踪指令`, `_叙述风格指令`) | 0 | 有ID/名称/始终触发/注入深度/内容，**无触发关键词** |
| `character/` | 14 | 1 (`_人物总览`) | 13 (Seraphina, 劳拉, 菲, 亚莉莎...) | 全字段 |
| `world/` | 14 | 1 (`_世界观概述`) | 13 (圣光, 鬼之力, 腐化...) | 全字段 |
| `creature/` | 11 | 1 (`_怪物与生物总览`) | 10 (影牙兽, 低语者, 圣歌鸟...) | 全字段 |
| `location/` | 20 | 1 (`_地点总览`) | 19 (心木树下, 圣光之泉...) | 全字段 |
| `npc/` | 4 | 1 (`_次要人物总览`) | 3 (Thalion, 古老先灵, 瓦利玛) | 全字段 |
| `event/` | 170 | 0 | 170 | 事件专用格式 |
| **总计** | **239** | **11** | **228** | — |

### 2.2 格式不一致问题

| # | 问题 | 影响 |
|---|------|------|
| 1 | `chapter/` 文件缺少 `触发关键词` 字段 | 构建脚本需特殊处理（始终触发条目不依赖key，字段可省略） |
| 2 | `chapter/` 文件ID前缀为`CH`，其他为`CHR`/`WLD`/`CRT`/`LOC`/`NPC` | 同目录内唯一即可，不影响构建 |
| 3 | `内容` 字段为bullet列表格式（`  - ...`），需合成段落 | 构建时去除bullet标记，保留层级结构 |
| 4 | 旧`event/05_事件系统.md`仍存在 | 已是生成产物，删除不依赖它 |
| 5 | `event/_TEMPLATE.TXT` 和 `_TEMPLATE_RULES.md` | 非条目文件，构建时跳过 |

### 2.3 需删除的旧文件

| 文件 | 原因 |
|------|------|
| `docs/05_事件系统.md` | 旧架构索引，已被TXT+assemble_md取代 |
| `docs/00_方案总览.md` | 旧架构文档，内容已分散到world/等子目录 |
| `docs/01_角色档案.md` ~ `04_关系阶段.md` | 同上 |
| `docs/06_条目规划与格式.md` ~ `08_事件格式标准.md` | 同上 |
| `scripts/archive/` 下过时脚本 | 已在V9归档 |

---

## 三、执行计划

### 阶段一：TXT格式标准化

| # | 任务 | 详情 |
|---|------|------|
| 1.1 | `chapter/` 文件补全 `触发关键词` 字段 | 始终触发条目的keywords留空数组，但字段行保留以统一解析格式。加 `触发关键词:` 行（空值） |
| 1.2 | 统一所有TXT的字段顺序 | `ID → 名称 → 触发关键词 → 始终触发 → 注入深度 → 内容`（chapter/文件同此顺序） |
| 1.3 | 检查所有TXT的 `始终触发` 值 | 确保 `_总览` 文件和 `chapter/` 文件为 `是`，其余为 `否` |
| 1.4 | 验证全部TXT字段完整性 | 写临时脚本检查：每个TXT有ID/名称/始终触发/注入深度/内容五个必填字段 |

### 阶段二：build_eldoria.py 完全重写

#### 2.1 核心设计

```
build_eldoria.py 新架构：

1. TXT扫描器 — 遍历 docs/ 子目录，收集所有TXT文件
   ├── 跳过 _TEMPLATE.TXT, _TEMPLATE_RULES.md
   ├── 跳过 event/备份/
   └── 识别TXT类型：event / non-event

2. TXT解析器 — 统一解析key: value格式
   ├── 通用解析器：适用于chapter/character/world/creature/location/npc
   └── 事件解析器：适用于event/{章节}（已有 _get_md_entries 逻辑）

3. 条目生成器 — TXT → JSON entry
   ├── 名称 → comment
   ├── 触发关键词 → key[] (逗号分隔trim)
   ├── 始终触发 → constant (是=true)
   ├── 注入深度 → depth (int)
   ├── 内容 → content (bullet列表合成段落)
   └── 自动分配: uid, order

4. UID分配策略
   ├── 始终触发条目 (constant=true): uid 0-10 (11条)
   ├── 角色/世界/生物/地点/NPC条目: uid 11-79 (69条)
   └── 事件条目: uid 80-249 (170条)

5. 输出: output/Eldoria_V10.0.0.json
```

#### 2.2 条目排序规则

```
1. chapter/      — 始终触发 (6条) — uid 0-5
2. world/_世界观概述 — 始终触发 (1条) — uid 6
3. character/_人物总览 — 始终触发 (1条) — uid 7
4. location/_地点总览 — 始终触发 (1条) — uid 8
5. creature/_怪物与生物总览 — 始终触发 (1条) — uid 9
6. npc/_次要人物总览 — 始终触发 (1条) — uid 10
7. world/        — 按需触发 (13条) — uid 11-23
8. character/    — 按需触发 (13条) — uid 24-36
9. location/     — 按需触发 (19条) — uid 37-55
10. creature/    — 按需触发 (10条) — uid 56-65
11. npc/         — 按需触发 (3条) — uid 66-68
12. event/{章节} — 按需触发 (170条) — uid 69-238
```

#### 2.3 实现要点

| 要点 | 说明 |
|------|------|
| `key` | 始终触发条目: `[]`。非始终触发: 从`触发关键词`逗号分隔提取 |
| `selective` | 始终触发条目: `true`。非始终触发: `true`（依靠key匹配） |
| `content` 合成 | bullet标记(`- `)保留以维持层级结构。bullet内容按原样保留缩进 |
| `depth` | 始终触发条目: 1-2。按需触发条目: 从`注入深度`读取(2-4) |
| `order` | 从100开始递增，同组内连续 |
| 概率/冷却 | 全部 `probability: 100, useProbability: true`，无冷却 |

#### 2.4 删除的旧代码

删除以下全部硬编码函数及其调用：
- `get_uid0_30_entries()` ~ `get_uid78_109_entries()` (约2000行)
- `get_common_entries()` / `get_ntrs_entries()` / `get_pure_entries()` 等（已删除）
- `_REGISTRY` 收集机制（不再需要）
- 旧的 `CHAR_DESC` / `CHAR_SCENARIO` 顶层字段（保留但简化）

### 阶段三：关联文件清理

| # | 任务 |
|---|------|
| 3.1 | 删除 `docs/05_事件系统.md`（旧索引，assemble_md可重新生成） |
| 3.2 | 归档旧的 `docs/0X_*.md` 文件到 `docs/archive/` |
| 3.3 | 更新 `assemble_md.py` — 如果05MD不再需要，考虑废弃 |
| 3.4 | 更新 `generate_event_browser.py` — 改为直接读TXT而非05MD |
| 3.5 | 更新 `README.md` 项目结构图 |
| 3.6 | 更新 `CLAUDE.md` 架构说明 |

### 阶段四：验证

| # | 任务 |
|---|------|
| 4.1 | `python scripts/build_eldoria.py --dry-run` — 构建成功，条目数正确(239条) |
| 4.2 | `python scripts/event_tool.py validate` — 零Rule1/5/8/9报错 |
| 4.3 | 对比新旧JSON输出：确认始终触发条目正确，事件条目完整 |
| 4.4 | 浏览器HTML正常渲染 |
| 4.5 | JSON可正常导入SillyTavern |

---

## 四、执行顺序

```
阶段一 (TXT标准化) → 阶段二 (build重写) → 阶段三 (清理) → 阶段四 (验证)
       ↓                    ↓                  ↓              ↓
  统一69个TXT格式      零硬编码构建器      删除旧MD文件    全流程通过
```

**依赖关系**：阶段一必须先完成（build依赖统一TXT格式）。阶段三可与阶段二并行。

---

## 五、注意事项

1. **构建脚本完全重写**：新旧JSON条目uid会完全变化——事件浏览器和下游工具需相应更新
2. **TXT格式是唯一权威源**：标准化后的TXT文件成为世界书条目的唯一来源。修改内容只需编辑TXT + rebuild
3. **始终触发条目**：chapter/全部 + 各子目录的`_总览`文件 = 11条始终触发。这些条目没有key匹配——始终在上下文中
4. **事件条目**：保持现有事件TXT格式不变。事件条目由`阶段`/`NSFW`等字段标记，非始终触发
5. **05_事件系统.md**：如果不再需要作为中间产物，assemble_md.py可废弃
6. **备份**：构建前自动备份旧JSON到backup/
7. **不引入新Rule6**：所有新增/修改的文字需通过Rule6检查

---

*任务书完。确认后从阶段一开始执行。*
