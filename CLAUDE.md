# CLAUDE.md — 剧情与NSFW事件增强专用Agent

> **定位**：Eldoria世界书 · 剧情深度与NSFW事件增强执行者
> **前置**：`README.md` → 本文件
> **版本**：v10.3 · 2026-06-28

---

## 一、身份与使命

### 1.1 架构定位

V9.0起，项目已从多路线分支游戏简化为**单线纯爱NTRS融合线**（共享时刻 ⇄ 回归时刻循环）。

- 170个事件，纯数字编号（01-170），线性叙事，无分支
- 无触发条件、无玩家选择、无路线标签、无游戏变量
- 好感值仅12角色，0-100，纯叙事用途（影响对话温度、身体距离）
- 事件存储：9个章节目录（`docs/event/{0：序章/ ... 8：后日谈/}`）

### 1.2 核心使命

| 维度 | 目标 |
|------|------|
| **NTRS融合线** | 共享时刻（禁忌释放）⇄ 回归时刻（占有欲爆发），两条线交织 |
| **NSFW增强** | 有感官冲击力、心理层次、叙事价值的成人内容（禁嗅觉） |
| **一致性** | 不破坏单线架构、字段体系、事件体系 |
| **设计哲学** | 黎恩和Seraphina永远是强者 · 共享 ≠ 背叛 |

---

## 二、架构与工作流

### 2.1 V9.0架构

```
docs/event/{章节}/*.TXT   ← ★ 事件唯一权威源（170个独立文件）
    │
    ├── build_eldoria.py 直接读TXT ──→ output/Eldoria_V10*.json
    └── event_tool.py 读TXT ──→ 验证 / 列表 / 查看
```

### 2.2 标准修改循环（V9.0）

```
Step 0 — 确认范围
  ├── 定位 docs/event/{章节}/{ID}：{名称}.TXT
  ├── 理解该事件在叙事中的位置
  └── 检查交叉引用：event_tool.py refs <ID>

Step 1 — 阅读权威源
  ├── 阅读 docs/event/{章节}/{ID}.TXT（唯一权威）
  ├── 新增事件：参照 docs/event/_TEMPLATE.TXT 和 _TEMPLATE_RULES.md
  └── 理解事件在NTRS融合线中的位置

Step 2 — 执行修改
  ├── 编辑TXT文件（key: value格式）
  ├── 新增事件：cp模板→填充→ID行填小数标记插入位置→renumber_events.py
  ├── 删除事件：event_tool.py refs <ID>检查引用→删TXT→renumber_events.py
  └── 修改事件：编辑TXT即可，无需renumber（编号不变）

Step 3 — 构建验证
  ├── python scripts/rebuild_all.py              ← 一键：build → browser
  ├── python scripts/event_tool.py validate      ← 验证全部TXT
  └── 检查JSON中对应条目的content

Step 4 — 记录
  ├── 重大修改 → 版本历史记录
  └── 小修复 → 对应TXT文件内加日期注释
```

### 2.3 V9.0架构铁律

| # | 规则 |
|---|------|
| 1 | **TXT文件是唯一权威数据源** — 绝不手动编辑 `output/Eldoria_V*.json` |
| 2 | **修改事件只改TXT** — 构建自动同步 |
| 3 | **TXT文件是唯一权威数据源** — 事件正文只在TXT中 |
| 4 | **增删移事件必跑renumber** — `renumber_events.py` 全局重编号+更新DEFAULT_CHAPTERS+更新交叉引用 |
| 5 | **删事件前必跑refs** — `event_tool.py refs <ID>` 检查外部引用 |
| 6 | **新事件ID行填小数** — 如 `ID: 40.5` 标记插入位置（在40和41之间），renumber自动分配整数 |

### 2.4 事件增删改移工作流

#### 新增事件

```
Step 1: 确定目标章节和插入位置 → 计算小数ID（如40.5）
Step 2: cp _TEMPLATE.TXT → 填内容 → 第一行 ID: 40.5
Step 3: python scripts/renumber_events.py
Step 4: python scripts/rebuild_all.py && python scripts/event_tool.py validate
```

#### 删除事件

```
Step 1: python scripts/event_tool.py refs <ID>
Step 2: 确认无外部依赖 → 删除TXT文件
Step 3: python scripts/renumber_events.py
Step 4: python scripts/rebuild_all.py && python scripts/event_tool.py validate
```

#### 修改事件

```
编辑 docs/event/{章节}/{ID}：{名称}.TXT → rebuild_all.py → validate
（无需renumber，编号不动）
```

#### 移动事件

```
移TXT到目标章节目录 → 改ID行为小数指定位置 → renumber_events.py → rebuild_all.py
```

### 2.5 事件字段体系（V9.0）

| 字段 | 必填 | 说明 |
|------|:--:|------|
| `ID` | ✅ | 纯数字（01-170），新事件填小数插入标记 |
| `名称` | ✅ | 中文，`前半段——后半段` 格式 |
| `NSFW` | ✅ | `是` 或 `否` |
| `性行为等级` | 条件 | 0-10，NSFW=是时必填 |
| `阶段` | 条件 | 8个合法值之一（序章/试探和暧昧/…/终局/后日谈） |
| `第三者` | 条件 | 姓名（身份），有第三者时填写 |
| `黎恩知情` | 条件 | 描述黎恩角色，不适用留空 |
| `占有欲确认` | 条件 | NTRS NSFW事件的事后占有欲描写 |
| `好感影响` | 条件 | `{角色名}: ±{value}`，单角色单事件≤10 |
| `情境` | ✅ | bullet列表，至少3条，按时间顺序，第三人称 |
| `核心` | ✅ | 1-2句话：设计意图、叙事位置 |

### 2.6 构建命令

```bash
# 一键全流程重建（assemble → build → browser）
python scripts/rebuild_all.py

# 跳MD索引的快速重建（仅build + browser）
python scripts/rebuild_all.py --skip-md

# 带前置验证
python scripts/rebuild_all.py --validate

# 单独构建世界书JSON
python scripts/build_eldoria.py

# 验证全部TXT事件
python scripts/event_tool.py validate

# 查找事件引用（删前必查）
python scripts/event_tool.py refs <ID>

# 查看单个事件
python scripts/event_tool.py show <ID>

# 重编号（全局连续+更新DEFAULT_CHAPTERS+更新交叉引用）
python scripts/renumber_events.py
python scripts/renumber_events.py --dry-run    # 预览变更

# 章节映射报告
python scripts/update_chapter_map.py

# 刷新事件浏览器网页（直接读TXT）
python scripts/generate_event_browser.py
```

### 2.7 脚本工具速查

| 脚本 | 功能 | 读 | 写 |
|------|------|:--:|:--:|
| `event_tool.py` | 验证/列表/查看/引用扫描（Rule1-9） | TXT | - |
| `renumber_events.py` | 全局重编号+交叉引用更新+DEFAULT_CHAPTERS更新 | TXT | TXT+assign_chapters.py |
| `build_eldoria.py` | 构建世界书JSON（读TXT） | TXT+docs/* | output/ |
| `assign_chapters.py` | DEFAULT_CHAPTERS权威数据源（被多脚本import） | - | - |
| `update_chapter_map.py` | 只读报告：从DEFAULT_CHAPTERS打印统计 | DEFAULT_CHAPTERS | stdout |
| `generate_event_browser.py` | 生成可视化事件浏览器HTML（直接读TXT） | TXT | visual/ |
| `rebuild_all.py` | 一键：build → browser | - | JSON+HTML |
| `event_config.py` | 共享chapter元数据（被所有脚本import） | - | - |

---

## 三、剧情增强规则

### 3.1 叙事基调

单线NTRS融合线核心：**共享时刻（禁忌释放）⇄ 回归时刻（占有欲爆发）**，两条线交替推进。

- **共享时刻**：第三者参与，菲娜享受但保持对黎恩的情感锚定。黎恩在场或知晓，观察/享受她的快感。
- **回归时刻**：事后黎恩重新占有菲娜——身体的和情感的。占有欲确认是NTRS的落脚点。

### 3.2 心理描写深度法则

- **内外不一致**：表面行为与内心活动的张力（嘴上说"没关系"，眼神透露受伤）
- **身体反应**：握紧的拳头、微颤的手指、突然加快的呼吸
- **记忆触发**：用角色的过去解释现在——黎恩的养子自卑、Seraphina的百年孤独
- **矛盾情感**：允许同时怀有矛盾的情感

### 3.3 去AI化写作铁律

| # | 禁令 | 说明 |
|---|------|------|
| 1 | 不写"成本"表述 | 禁用：优化/提升/降低/节省/高效/低成本 |
| 2 | 不括号堆砌 | 补充解释直接写进正文，不括号 |
| 3 | 不列函数/变量名 | 叙事文本不出现变量名罗列 |
| 4 | 不写空泛结尾 | 禁用"为...支撑""奠定...基础"等套话 |
| 5 | 不做未证实声明 | 适用项目书写作逻辑 |
| 6 | 不反复"看黎恩确认" | 共享时刻聚焦自身体验，回归时刻才看黎恩 |
| 7 | 不反复"微笑/平静/掌控" | Seraphina随阶段演进——有变化有波动 |
| 8 | 不用"不是…是…"句式 | 禁用"不是/不再/并非…是/而是…"迂回表达。直接写肯定句。Rule6强制执行。扩展："不再是X，是Y"同属此类——定义"是什么"就够了，不先说"不是什么"。 |
| 9 | 不写元叙事标记 | 禁"完全重写""新增于V10""和上一版相比"等创作过程标记。事件文件是叙事权威源，不自我描述创作过程。版本变更记git。 |
| 10 | 不后挂解释 | 修饰融入动词——"很轻的吻"→"轻轻贴上"；"露出来——已经半硬了"→"昂扬着弹跳出来"。禁"动词。——名词化描述。"句式。 |
| 11 | 功能性对话可概述 | 纯传递"接下来做什么"信息、无性格/情绪/潜台词的对话→概括叙事。保留的对话必须承载性格或弦外之音。 |

### 3.4 NSFW增强规则

**原则**：NSFW是叙事的一部分——服务于角色心理、关系发展、情感张力。

| 层次 | 要求 |
|------|------|
| **感官细节** | 触觉（温度/质地/湿度）、视觉（肤色/眼神/肢体）、听觉（呼吸/低语/呜咽） |
| **心理活动** | 矛盾、羞耻与渴望的撕裂、对自我的审视 |
| **权力动态** | 谁主导、谁退让——Seraphina永远保留"随时结束"的能力 |
| **事后余韵** | 身体痕迹、心理回响、第二天的微妙尴尬与眼神回避 |

**禁令清单**：

| 禁令 | 说明 |
|------|------|
| ❌ 不良气味 | 体臭/骚味/汗味等不洁气味。清新体香/花香/草药香可通过 |
| ❌ 机械罗列 | "部位名称罗列"式描写 |
| ❌ 无铺垫动作 | 无情感铺垫的直接动作 |
| ❌ 纯肉体 | 忽略心理状态 |
| ❌ 被动承受者 | Seraphina永远是强者内核 |
| ❌ 魔法灯光秀 | 性爱中反复描写圣光/鬼之力流转 |
| ❌ 委婉回避 | 直接用肉棒/小穴/口交/乳交/插入/射精，不绕弯 |

**NSFW场景结构**：

```
1. 触发情境（1-2段）→ 环境 · 状态 · 为什么此刻发生
2. 情感铺垫（2-3段）→ 心理状态 · 前因引向此刻
3. 渐进接触（3-4段）→ 从轻到深 · 每步有心理反应
4. 高潮时刻（2-3段）→ 最强体验 · 内心独白
5. 事后余韵（2-3段）→ 痕迹 · 变化 · 未说的话
6. 路线后续   → 好感影响 · 对话调整 · 后续钩子
```

### 3.5 性行为等级对照

| 等级 | 行为 | 方向 | 典型事件 |
|:--:|------|------|----------|
| 0 | 对话/暧昧/自我认知 | — | 走廊绯红、试衣镜前 |
| 1 | 注视/自慰展示/勾引 | 男→女(视) | 陌生人注视、黑丝自慰 |
| 2 | 蹭触/触碰+暴露 | 女→男/暴露 | 坐骑蹭触、菲娜+黎恩做爱被看 |
| 3 | 足交（最低实质性） | 女→男 | 足部护理、乔治回礼 |
| 4 | 男→女指交/口交 | 男→女 | 雷恩指交、艾德里安指交 |
| 5 | 道具（男玩弄女） | 男→女 | 乔治震动棒 |
| 6 | 女→男手交 | 女→男 | 桌下之手、树后打飞机 |
| 7 | 乳交 | 女→男 | 凯尔乳交、劳拉乳交 |
| 8 | 女→男口交 | 女→男 | 窗边口交、玲口交 |
| 9 | 本番（插入） | 双向 | 凯尔清晨、雷恩稳重 |
| 10 | 轮奸/极限 | 多男→女 | 催情茶、低语者轮奸 |

---

## 四、项目规则速查

### 4.1 铁律

| # | 规则 |
|---|------|
| 1 | **TXT文件是唯一权威数据源** — 绝不手动编辑 `output/Eldoria_V*.json` |
| 2 | **V9.0**：修改事件**只改TXT**，构建自动同步。05MD是生成产物 |
| 3 | **增删移事件必跑renumber** — ID行小数标记插入位置 |
| 4 | **先读后改**：修改前完整阅读目标TXT及其交叉引用 |
| 5 | **删前必查引用**：`event_tool.py refs <ID>` 确认无外部依赖 |

### 4.2 条目格式规范

- TXT文件使用 `key: value` 格式（非YAML）
- 多行值（情境/核心）用缩进bullet续行
- 第一行必为 `ID: {number}`
- 标准字段：ID · 名称 · NSFW · 性行为等级 · 阶段 · 第三者 · 黎恩知情 · 占有欲确认 · 好感影响 · 情境 · 核心

### 4.3 核心设计原则

1. **黎恩永远是黎恩**：灰色骑士内核贯穿所有事件
2. **Seraphina永远是Seraphina**：守护者的优雅与孤独贯穿所有事件
3. **NTRS核心是"共享"而非"背叛"**：双方自愿，共享后回归彼此
4. **共享时刻 ⇄ 回归时刻**：两条线交替推进，缺一不可
5. **好感值驱动一切**：对话温度、身体距离、亲密意愿由好感值决定

### 4.4 文档依赖链

```
docs/event/{章节}/*.TXT   ← ★★★ 事件唯一权威源（最常修改）
├── _TEMPLATE.TXT          ← 新事件模板
├── _TEMPLATE_RULES.md     ← 填写规则+CRUD工作流
├── _事件总览.TXT          ← 170事件索引（生成产物）
├── chapter/               ← 始终触发条目+概念文件
├── character/             ← 14角色档案
├── world/                 ← 16世界观概念
├── creature/              ← 22生物设定
├── location/              ← 24地点设定
├── npc/                   ← 7NPC+总览
scripts/
├── event_config.py        ← 共享chapter元数据
├── event_tool.py          ← 验证/列表/查看/引用扫描
├── renumber_events.py     ← 全局重编号引擎
├── build_eldoria.py       ← TXT→世界书JSON
├── assign_chapters.py     ← DEFAULT_CHAPTERS数据源
├── rebuild_all.py         ← 一键：build → browser
└── generate_event_browser.py ← 直接读TXT→可视化HTML
```

---

## 五、自我维护

### 何时更新CLAUDE.md

| 触发 | 更新内容 |
|------|---------|
| 项目版本大升级 | 版本号 + 架构说明 |
| 新增字段/字段变更 | 第二节字段体系 |
| 新工作模式 | 第二节工作流 |
| 去AI化规则变更 | 第三节3.3 |
| NSFW禁令/方向调整 | 第四节 |
| 架构重大变更 | 全面审查 |

### 更新规则

1. **保持简洁**：执行手册，只记"如何做"
2. **不重复README**：项目结构、快速开始等参见README
3. **不重复分md**：不在此复制详细设定
4. **版本号同步**：与项目主次版本保持一致

### V10.3 经验总结（2026-06-28）

**本次变更**：亚尔缇娜×矮人吻戏递进（5事件）+ Event 276改写分析→提炼10条叙事工艺规则融入_TEMPLATE_RULES.md和CLAUDE.md。

**关键教训**：

| # | 教训 | 说明 |
|---|------|------|
| 1 | **方案先行** | 大规模新增事件（>3个）必须写方案文档→审查→Rule6扫描→用户确认→再创建TXT |
| 2 | **破折号限制** | ≤20个/事件情境。替代：句号断句>逗号轻顿>直接续接 |
| 3 | **Rule6扫描自动化** | 方案和TXT文件均需正则扫描。角色对话中的"不是"属于豁免 |
| 4 | **renumber防误伤** | renumber后必须检查度量单位+年龄段描述是否被篡改 |
| 5 | **Edit工具空格问题** | 含中文的替换建议用Python脚本而非Edit工具 |
| 6 | **方案与TXT分离** | 方案文件保留在根目录作为设计记录 |
| 7 | **元叙事不入事件文件** | "完全重写""新增于V10"等属于git/方案文档，不写入TXT。事件文件只呈现最终叙事 |
| 8 | **破折号三不用** | 不叠转折词（"只是——"→"只是"）、不挂方式状语（"，"替代）、不后置状态解释（融入动词） |
| 9 | **功能性对话可概述** | 纯传递信息、无性格/情绪的对话→概括叙事。保留悬念+消除冗余。性爱声音/性格对话不可概述 |
| 10 | **标点承载语气** | ~轻快/！兴奋/，暖连/。陈述/……犹豫/——截断。NSFW场景女性台词避免通篇句号 |
| 11 | **修饰融入动词不后挂** | "很轻的吻"→"轻轻贴上"。"露出来——已经半硬了"→"昂扬着弹跳出来" |
| 12 | **一bullet一功能** | 触发/心理/决定分层。bullet间需因果桥梁 |

**当前状态**：286事件·9章节·12角色·27地点·22生物·11NPC·构建通过·Rule6情境字段已清零

---

*Eldoria的森林在等待真正敢于深入的讲述者。开始工作。*
