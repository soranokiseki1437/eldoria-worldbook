# CLAUDE.md — 世界书构建与章节增强专用Agent

> **定位**：Eldoria世界书 · 100%对齐俺妹ver1.41格式
> **版本**：v10.3 · 2026-06-29

---

## 一、架构与格式

### 1.1 输出格式

**JSON格式100%照抄俺妹ver1.41**，逐字段对齐：

```
顶层：entries + originalData（双向冗余）
条目：43字段 + extensions（snake_case） + characterFilter
      字段顺序与参考文件完全一致，不剥离任何字段
group：全部 ""
```

### 1.2 章节体系

- 302个章节，纯数字编号，线性叙事，无分支
- 9个阶段目录（`docs/event/{0：序章/ ... 8：后日谈/}`）
- 章节key：`['第N章']` — 单key，`selectiveLogic: 0`（OR匹配）
- `{{user}}` = 黎恩·舒华泽（第三人称叙事）

### 1.3 系统条目（照抄俺妹）

| 条目 | 位置 | 说明 |
|:--|:--|:--|
| `章节追踪指令` | pos=4, order=999 | 玩家上下文驱动的剧情重构 |
| `游戏状态界面` | pos=4, order=998 | `<overall>` 格式模板 |
| `叙述风格指令` | pos=4, order=100 | 第三人称+去AI化铁律 |

---

## 二、工作流

### 2.1 标准修改循环

```
Step 0 — 确认范围
  ├── 定位 docs/event/{章节}/{ID}：{名称}.TXT
  └── 检查交叉引用：event_tool.py refs <ID>

Step 1 — 阅读权威源
  ├── 阅读 TXT（唯一权威）
  └── 新增章节：参照 docs/event/_TEMPLATE.TXT

Step 2 — 执行修改
  ├── 编辑TXT文件（key: value格式）
  ├── 新增：cp模板→填充→ID行填小数→renumber_events.py
  ├── 删除：event_tool.py refs <ID>→删TXT→renumber_events.py
  └── 修改：编辑TXT即可，无需renumber

Step 3 — 构建验证
  ├── python scripts/build_eldoria.py           ← 构建JSON
  ├── python scripts/event_tool.py validate     ← 验证TXT
  └── 检查JSON中对应条目的content

Step 4 — 记录
  ├── 重大修改 → git commit
  └── 小修复 → TXT文件内加日期注释
```

### 2.2 章节TXT字段体系

| 字段 | 必填 | 说明 |
|------|:--:|------|
| `ID` | ✅ | 纯数字，新章节填小数插入标记 |
| `名称` | ✅ | 中文，`前半段——后半段` 格式 |
| `NSFW` | ✅ | `是` 或 `否` |
| `性行为等级` | 条件 | 0-10，NSFW=是时必填 |
| `阶段` | 条件 | 8个合法值（序章/…/后日谈） |
| `第三者` | 条件 | 姓名（身份），有第三者时填写 |
| `黎恩知情` | 条件 | 描述黎恩角色 |
| `占有欲确认` | 条件 | NTRS NSFW章节事后描写 |
| `好感影响` | 条件 | `{角色名}: ±{value}`，≤10 |
| `情境` | ✅ | bullet列表，至少3条，第三人称 |
| `核心` | ✅ | 设计意图、叙事位置 |
| `章节核心目标` | 新 | ← `核心` 自动推导（构建脚本填充） |
| `章节任务` | 新 | ← LLM逐章写入TXT，构建脚本直接读取 |
| `章节终止条件` | 新 | ← LLM逐章写入TXT，构建脚本直接读取 |

### 2.3 构建命令

```bash
python scripts/build_eldoria.py              # 构建JSON（自动备份）
python scripts/build_eldoria.py --dry-run    # 仅验证不写盘
python scripts/rebuild_all.py                # build → browser
python scripts/event_tool.py validate        # 验证全部TXT
python scripts/event_tool.py refs <ID>       # 查引用（删前必跑）
python scripts/renumber_events.py            # 全局重编号
python scripts/renumber_events.py --dry-run  # 预览变更
```

### 2.4 章节内容生成格式

构建脚本自动将TXT字段包装为俺妹格式：

```
<章节剧情>
[章节编号]: 第N章
[章节标题]: 第N章：标题
[章节核心目标]:
核心字段内容
[章节任务]:
自动推导的一句话任务
[章节终止条件]:
1.条件1
2.条件2
[补充信息]:
[NSFW]: 是/否
[阶段]: 阶段名
...
</章节剧情>
```

---

## 三、叙事与写作规则

### 3.1 叙事人称（照抄俺妹）

- **第三人称**，`{{user}}`（黎恩·舒华泽）为视角焦点
- 始终用"他"或"黎恩"指代{{user}}，不用"我"
- {{user}}不知道的事情不写，保持视角一致性
- 其他角色内心通过{{user}}的观察推测

### 3.2 叙事基调

单线NTRS融合线：**共享时刻 ⇄ 回归时刻**交替推进。
Seraphina永远是强者——共享是双方同意的体验，回归是情感落点。

### 3.3 去AI化写作铁律

| # | 禁令 | 说明 |
|---|------|------|
| 1 | 不写"成本"表述 | 禁用优化/提升/降低/节省/高效/低成本 |
| 2 | 不括号堆砌 | 补充解释直接写进正文 |
| 3 | 不列函数/变量名 | 叙事文本不出现变量名罗列 |
| 4 | 不写空泛结尾 | 禁用"为...支撑""奠定...基础" |
| 5 | 不做未证实声明 | 适用项目书写作逻辑 |
| 6 | 不反复"看黎恩确认" | 共享时刻聚焦体验，回归时刻才看黎恩 |
| 7 | 不反复"微笑/平静/掌控" | Seraphina随阶段演进有变化有波动 |
| 8 | 不用"不是…是…"句式 | 直接写肯定句。Rule6强制执行 |
| 9 | 不写元叙事标记 | 版本变更记git，不入TXT |
| 10 | 不后挂解释 | 修饰融入动词——"轻轻贴上" |
| 11 | 功能性对话可概述 | 保留承载性格或弦外之音的对话 |

### 3.4 NSFW规则

- 直接词汇：肉棒/小穴/口交/乳交/插入/射精
- 禁不良气味（体臭/骚味）——足部气味例外（好闻的、色情的）
- 禁魔法灯光秀（圣光/鬼之力流转）
- Seraphina永远是强者内核

### 3.5 `<overall>` 状态格式（照抄俺妹）

```
<overall>
<chapter_information>
当前章节|第x章 章节名称
下一章节|第x+1章
过去经历的章节|第y章,第y+1章...
章节任务|任务描述
章节终止条件|1.条件1\n2.条件2
在场人物|{{user}},角色1,角色2
</chapter_information>
<StatusBlock>
```
🕣时间 | 🌏地点 | ☁️天气
# 角色名 年龄
╒═════
💖对{{user}}的好感度:{`love_point`}
📅当前章节:
👚 服装:
💭 情绪:
💑 行为:
🤔 对{{user}}的想法:
🙀 Tips:
╘═════
```
</StatusBlock>
</overall>
```

- 下一章节只写编号（`第x+1章`），不写名称
- 📅当前章节必须包含章节全名
- 男性角色 `💖` 改为 `🤝对{{user}}的信赖度`

---

## 四、核心设计原则

1. **黎恩永远是黎恩**：灰色骑士内核，`{{user}}`=黎恩
2. **Seraphina永远是Seraphina**：守护者的优雅与孤独
3. **共享 ≠ 背叛**：双方自愿，共享后回归彼此
4. **共享时刻 ⇄ 回归时刻**：两条线交替推进
5. **章节TXT是唯一权威源**：绝不手动编辑JSON

---

## 五、文档依赖链

```
docs/event/{章节}/*.TXT     ← ★ 章节唯一权威源
├── _TEMPLATE.TXT            ← 新章节模板
├── chapter/                 ← 系统指令+概念文件
│   ├── _章节追踪指令.TXT    ← 玩家上下文驱动的重构流程
│   ├── _游戏状态界面.TXT    ← <overall>格式模板
│   └── _叙述风格指令.TXT    ← 第三人称叙事+去AI化
├── character/               ← 角色档案
├── world/                   ← 世界观概念
├── magic/                   ← 魔法体系
├── creature/                ← 生物设定
├── location/                ← 地点设定
├── npc/                     ← NPC档案
├── affection/               ← 好感度分级
scripts/
├── build_eldoria.py         ← TXT→世界书JSON（逐字段对齐俺妹格式）
├── event_tool.py            ← 验证/列表/查看/引用扫描
├── renumber_events.py       ← 全局重编号引擎
└── rebuild_all.py           ← 一键重建
开场白.txt                   ← 首个消息（含<overall>状态块）
output/Eldoria_V10.3.0.json  ← 派生产物，不可手动编辑
```

---

## 六、V10.3 变更记录（2026-06-29）

**本次变更**：SillyTavern导入格式全面修复——100%对齐俺妹ver1.41。

| # | 变更 | 说明 |
|---|------|------|
| 1 | **JSON格式照抄俺妹** | extensions包装+originalData+全字段显式保留+group全空 |
| 2 | **"事件"→"章节"** | 129个TXT源文件321处替换，系统条目+keys+标签全部更新 |
| 3 | **章节key简化** | 6变体→单key `['第N章']`，selectiveLogic:0（OR匹配） |
| 4 | **章节内容加三字段** | `[章节核心目标]` 脚本自动填充 + `[章节任务]` `[章节终止条件]` LLM逐章写入TXT |
| 5 | **状态格式对齐** | 当前章节\|第x章 / 下一章节\|第x+1章 / 好感度Lv.x\|值/100\|{描述} |
| 6 | **叙事人称修正** | 第三人称，`{{user}}`=黎恩，视角一致性铁律 |
| 7 | **系统条目照抄** | 章节追踪指令+游戏状态界面+叙述风格指令——逐字段对齐参考 |
| 8 | **默认值不剥离** | 步骤4.5移除——selective/selectiveLogic/triggers/characterFilter全部保留 |
| 9 | **开场白** | `开场白.txt`含完整`<overall>`状态块 |

**当前状态**：434条目·302章节·9阶段·12角色·构建通过·格式100%对齐俺妹ver1.41

---

*Eldoria的森林在等待真正敢于深入的讲述者。开始工作。*
