# CLAUDE.md — 世界书构建与章节增强专用Agent

> **定位**：Eldoria世界书 · 100%对齐俺妹ver1.41格式
> **版本**：v10.5 · 2026-07-01

---

## 一、架构与格式

### 1.1 JSON输出格式

**13字段精简Object**：entries为Object（keyed by string uid），无extensions/originalData/characterFilter膨胀。

```
条目字段：uid / key / keysecondary / comment / content / constant /
         selective / order / position(0/1/4) / depth / group /
         excludeRecursion / preventRecursion
```

> **致命陷阱**：entries必须是 `{"0":{...}}`（Object），绝不可用 `[{...}]`（Array）——酒馆拒绝导入。

### 1.2 递归控制字段（V10.5新增）

| 字段 | 含义 | 章节 | 概念条目(角色/地点/生物等) | constant系统条目 |
|------|------|:--:|:--:|:--:|
| `excludeRecursion` | 不可递归：不被其他条目递归激活 | `true` | `true` | `false` |
| `preventRecursion` | 防止进一步递归：激活后不触发下级扫描 | `false` | `true` | `false` |

> 设计来源：俺妹ver1.41 — 章节只被key直接触发但可触发角色/地点条目；概念条目是终端节点不触发任何下级递归。

### 1.3 章节体系

- 350章，纯数字编号，线性叙事，无分支
- 9阶段：`docs/story/{0：序章/ ... 8：后日谈/}`
- 章节key：`['第N章']`，`selectiveLogic: 0`
- `{{user}}` = 黎恩·舒华泽（第三人称叙事）

### 1.4 系统条目

| 条目 | 位置 | 说明 |
|:--|:--|:--|
| `章节追踪指令` | pos=4 depth=0 order=999 | 章节上下文 |
| `游戏状态界面` | pos=4 depth=0 order=998 | `<overall>` 模板 |
| `叙述风格指令` | pos=4 depth=1 order=100 | 去AI化铁律 |
| `世界时间并行和隐奸` | pos=4 depth=1 order=100 | 时间/双视角/隐奸逻辑 |

### 1.5 章节Sex类型索引

**手工维护**：`docs/story/_sex_index.txt` — 每个NSFW章节的性行为类型标签。

```text
格式: ## 标签名 (N章)
       章节号: 标题
```

- **唯一权威源**，浏览器脚本只读不写
- 同一章节可出现在多个分类下
- 修改后运行 `python scripts/generate_chapter_browser.py` 更新浏览器
- 新增章节未分类时自动归入"未分类"标签

当前标签：本番/肛交/口交/乳交/腿交/足交/手交/指交/蹭穴/接吻/暴露/触碰/隐奸/群交/未分类

### 1.6 章节浏览器

`visual/全章节浏览器.html` — 双维度筛选：

- **👤 角色**：第三者字段 + 好感影响 + 标题 三层OR提取
- **🏷️ 类型**：读取 `_sex_index.txt` 手工索引
- 两个维度可AND组合，点角色→点类型→精准定位

---

## 二、工作流

### 2.1 标准修改循环

```
Step 0  定位 docs/story/{阶段}/{ID}：{名称}.TXT
Step 1  阅读TXT（唯一权威源），新增参照 _TEMPLATE.TXT
Step 2  编辑TXT → python scripts/build_eldoria.py 构建JSON
Step 3  python scripts/story_tool.py validate 验证
Step 4  git commit
```

### 2.2 章节TXT字段体系

| 字段 | 必填 | 说明 |
|------|:--:|------|
| `ID` | ✅ | 纯数字 |
| `名称` | ✅ | `前半段——后半段` 格式 |
| `NSFW` | ✅ | `是` 或 `否` |
| `性行为等级` | 条件 | 0-10 |
| `阶段` | ✅ | 序章/…/后日谈 |
| `第三者` | 条件 | 姓名（身份） |
| `黎恩知情` | 条件 | 描述黎恩角色 |
| `好感影响` | 条件 | `{角色名}: ±{value}` |
| `情境` | ✅ | bullet列表，≥3条 |
| `核心` | ✅ | 设计意图 |
| `章节任务` | ✅ | 1句话核心任务 |
| `章节终止条件` | ✅ | 条件列表 |

> 新增章节后记得更新 `_sex_index.txt`，未确定类型先放"未分类"。

### 2.3 构建命令

```bash
python scripts/build_eldoria.py              # 构建JSON
python scripts/rebuild_all.py                # JSON + 浏览器一键重建
python scripts/generate_chapter_browser.py   # 仅重建浏览器
python scripts/story_tool.py validate        # 验证TXT
python scripts/story_tool.py refs <ID>       # 查引用
python scripts/renumber_events.py            # 全局重编号
```

---

## 三、叙事与写作规则

### 3.1 叙事视角

**第三人称多视角叙事**——黎恩·舒华泽（{{user}}）与Seraphina双主角，两人均为合法视角持有者。

- **黎恩视角（默认主轴）**：大部分叙事以黎恩为视角焦点。外部世界通过他的感官和内心呈现——看到的、听到的、闻到的、触摸到的，全部经过神经系统再输出。表面与内心的张力、矛盾情感、记忆触发、身体叙事构成叙事主质地
- **Seraphina视角（关键节点）**：共享时刻/隐奸/回归时刻等关键节点可切换至她的视角——她在第三者触碰时对黎恩的情感锚定、汇报时的心理状态、设计隐奸时的内心运作——这些是仅黎恩视角无法抵达的深度
- **视角切换规则**：同一回复内可有限切换，须有明确叙事目的；切换后首句明确当前视角持有者；无标记时默认回落黎恩视角；视角持有者不知道的事情不写
- **指代**：始终用"他"或"黎恩"指代{{user}}，不用"我"

完整规则详见 `docs/chapter/_叙述风格指令.TXT`。

### 3.2 叙事基调

单线NTRS融合线：**共享时刻 ⇄ 回归时刻**交替推进。
Seraphina永远是强者——共享是双方同意的体验，回归是情感落点。

### 3.3 去AI化写作铁律

| # | 禁令 | 说明 |
|---|------|------|
| 1 | 不写"成本"表述 | 禁用优化/提升/降低/节省/高效/低成本 |
| 2 | 不括号堆砌 | 补充解释直接写进正文 |
| 3 | 不列函数/变量名 | 叙事文本不出现变量名罗列 |
| 4 | 不写空泛结尾 | 禁用"为...支撑""奠定...基础" |
| 5 | 不做未证实声明 | |
| 6 | 不反复"看黎恩确认" | 共享时刻聚焦体验，回归时刻才看黎恩 |
| 7 | 不反复"微笑/平静/掌控" | Seraphina随阶段演进有变化有波动 |
| 8 | 不用"不是…是…"/"没有…只是…" | 直接写肯定句。已全局扫描杀26处 |
| 9 | 不写元叙事标记 | 版本变更记git，不入TXT |
| 10 | 不后挂解释 | 修饰融入动词——"轻轻贴上" |
| 11 | 功能性对话可概述 | 保留承载性格或弦外之音的对话 |

### 3.4 NSFW规则

- 直接词汇：肉棒/小穴/口交/乳交/插入/射精
- 禁不良气味（体臭/骚味）——足部气味例外
- 禁魔法灯光秀（圣光/鬼之力流转）
- Seraphina永远是强者内核

---

## 四、核心设计原则

1. **黎恩永远是黎恩**：灰色骑士内核，`{{user}}`=黎恩
2. **Seraphina永远是Seraphina**：守护者的优雅与孤独
3. **共享 ≠ 背叛**：双方自愿，共享后回归彼此
4. **共享时刻 ⇄ 回归时刻**：两条线交替推进
5. **章节TXT是唯一权威源**：绝不手动编辑JSON
6. **_sex_index.txt是类型权威源**：手工维护，脚本只读

---

## 五、文档依赖链

```
docs/story/{章节}/*.TXT        ← ★ 章节唯一权威源
├── _TEMPLATE.TXT                ← 新章节模板
├── _sex_index.txt               ← 手工维护的sex类型索引
├── chapter/                     ← 系统指令
│   ├── _章节追踪指令 / _游戏状态界面 / _叙述风格指令 / _世界时间并行和隐奸
├── character/ world/ magic/ creature/ location/ npc/ affection/

scripts/
├── build_eldoria.py              ← TXT→JSON
├── generate_chapter_browser.py   ← 章节浏览器（读索引+角色提取）
├── story_tool.py                 ← 验证/引用扫描
├── renumber_events.py            ← 全局重编号
└── rebuild_all.py                ← 一键重建

output/Eldoria_V10.5.0.json       ← 派生产物，不可手动编辑
visual/全章节浏览器.html           ← 章节浏览器
```

---

## 六、角色特征速查

| 角色 | 关键外貌 | 关键行为 |
|------|---------|---------|
| **乔治** | 棕色短发，**不戴眼镜** | 紧张摸后脑勺/铅笔滚落，社交腼腆技术自信 |
| **凯尔** | 深蓝短发，戴细框眼镜，异色瞳 | 常说"可以吗"，把性当研究，社交腼腆学术自信 |
| **艾玛** | 李子色长辫，薄荷蓝瞳 | 指尖捻辫梢=思考/解辫=交付，用学术术语包装情感 |
| **黎恩** | **黑发**，青紫瞳，鬼之力刻印 | 磨太刀=冥想，手指穿过她发丝是安抚 |
| **Seraphina** | **粉发**，琥珀瞳，无尖耳 | 走路无声，害羞把脸埋起来，战斗站桩施法 |
| **菲** | 白发，黄绿瞳，150cm | 沉默寡言，行动先于言语，拉黎恩衣角 |
| **亚尔缇娜** | 银白短发，深蓝瞳，142cm | 说话像念报告，黑丝，数据库式思考 |
| **奥蕾莉亚** | 银发，紫瞳，178cm G杯 | 绝对自信，什么都不害羞，温泉狂热 |
| **多尔金** | 矮人，暗棕乱发，148cm | 三字封顶，锻造专注，"不错"=最高评价 |
| **哈根** | 矮人，棕短发，灰蓝眼，142cm | 直率粗放大嗓门，心软，做事时唱矮人古歌 |
| **法林** | 矮人，暗棕长辫，深绿眼，139cm | 憨厚单纯，说"请""谢谢"，指尖微光 |
| **罗恩** | 狼人，绯红眼，183cm(满月205) | 温文尔雅，害羞敲额头，尾巴画弧=舒服 |
| **雷恩** | 深棕短发，深绿瞳，188cm | 话少且慢，磨剑=冥想，从不反问 |

---

## 七、V10.5 变更记录（2026-07-01）

| # | 变更 | 说明 |
|---|------|------|
| 1 | **递归控制字段** | 11→13字段，新增`excludeRecursion`+`preventRecursion` |
| 2 | **章节不可递归** | 395章：excludeRec=true, preventRec=false（可触发下级条目） |
| 3 | **概念条目双true** | 120条角色/地点/生物等：excludeRec=true, preventRec=true（终端节点） |
| 4 | **constant系统条目** | 14条始终激活：双false（递归机制不适用） |

### 历史版本

<details>
<summary>V10.3 变更记录（2026-06-29）</summary>

| # | 变更 | 说明 |
|---|------|------|
| 1 | **JSON瘦身** | 43字段→11字段，去extensions/originalData/characterFilter |
| 2 | **event→story改名** | 目录/脚本/浏览器全面重命名 |
| 3 | **章节任务+终止条件** | 全部350章补写 |
| 4 | **乔治不戴眼镜** | 23章"推眼镜"→"摸后脑勺" |
| 5 | **Rule8全局修复** | 杀26处"没有X只是Y"否定句式 |
| 6 | **世界时间并行** | pos=0→pos=4 depth=1 |
| 7 | **章节浏览器** | 角色×类型双维度筛选 |
| 8 | **sex类型索引** | 手工维护 `_sex_index.txt`，15标签+未分类 |

</details>

**当前状态**：529条目·395章节·9阶段·构建通过·JSON 0.8MB·浏览器双维度筛选·sex索引全覆盖·递归控制13字段

---

*Eldoria的森林在等待真正敢于深入的讲述者。开始工作。*
