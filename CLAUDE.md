# CLAUDE.md — 世界书构建与章节增强专用Agent

> **定位**：Eldoria世界书 · 100%对齐俺妹ver1.41格式
> **版本**：v10.15.0 · 2026-07-23 · 669章·807条目·9阶段·JSON 1.23MB
> **权威规则**：`docs/story/_TEMPLATE_RULES.md`（写作规则全集）· `docs/story/_短对话修复方案.md`（对话修复）· `docs/story/_条件修复方案.md`（终止条件/章节任务修复）

---

## 一、架构与格式

### 1.1 JSON输出格式

**13字段精简Object**：entries为Object（keyed by string uid），无extensions/originalData/characterFilter膨胀。

```
条目字段：uid / key / keysecondary / comment / content / constant /
         selective / order / position(0/1/4) / depth / group /
         excludeRecursion / preventRecursion
```

> **致命陷阱**：entries必须是 `{"0":{...}}`（Object），绝不可用 `[{...}]`（Array）——酒馆拒绝导入。

### 1.2 递归控制字段（V10.5新增）

| 字段 | 含义 | 章节 | 概念条目(角色/地点/生物等) | constant系统条目 |
|------|------|:--:|:--:|:--:|
| `excludeRecursion` | 不可递归：不被其他条目递归激活 | `true` | `true` | `false` |
| `preventRecursion` | 防止进一步递归：激活后不触发下级扫描 | `false` | `true` | `false` |

> 设计来源：俺妹ver1.41 — 章节只被key直接触发但可触发角色/地点条目；概念条目是终端节点不触发任何下级递归。

### 1.3 章节体系

- 624章，纯数字编号，线性叙事，无分支
- 9阶段：`docs/story/{0：序章/ ... 8：后日谈/}`
- 章节key：`['第N章']`，`selectiveLogic: 0`
- `{{user}}` = 叙事摄影机（玩家操控的叙事镜头，非任何角色）

### 1.4 系统条目

| 条目 | 位置 | 说明 |
|:--|:--|:--|
| `章节追踪指令` | pos=4 depth=0 order=999 | 章节上下文 |
| `游戏状态界面` | pos=4 depth=0 order=998 | `<overall>` 模板 |
| `写作与视角指令` | pos=4 depth=1 order=100 | 摄影机模型·视角选择·写作风格·去AI化铁律 |
| `世界时间并行和隐奸` | pos=4 depth=1 order=100 | 世界时间并行·隐奸逻辑 |
| `自由探索` | pos=4 depth=1 order=100 | 自由探索状态指令 |

### 1.5 章节Sex类型索引

**手工维护**：`docs/story/_sex_index.txt` — 每个NSFW章节的性行为类型标签。

```text
格式: ## 标签名 (N章)
       章节号: 标题
```

- **唯一权威源**，浏览器脚本只读不写
- 同一章节可出现在多个分类下
- 修改后运行 `python scripts/generate_chapter_browser.py` 更新浏览器
- 新增章节未分类时自动归入"未分类"标签

当前标签：本番/肛交/口交/乳交/腿交/足交/手交/指交/蹭穴/接吻/暴露/触碰/隐奸/群交/未分类

### 1.6 章节浏览器

`visual/全章节浏览器.html` — 双维度筛选：

- **👤 角色**：第三者字段 + 好感影响 + 标题 三层OR提取
- **🏷️ 类型**：读取 `_sex_index.txt` 手工索引
- 两个维度可AND组合，点角色→点类型→精准定位

---

## 二、工作流

### 2.1 标准修改循环

```
Step 0  定位 docs/story/{阶段}/{ID}：{名称}.TXT
Step 1  阅读TXT（唯一权威源），新增参照 _TEMPLATE.TXT
Step 2  编辑TXT → python scripts/build_eldoria.py 构建JSON
Step 3  python scripts/story_tool.py validate 验证
Step 4  git commit
```

### 2.2 章节TXT字段体系

| 字段 | 必填 | 说明 |
|------|:--:|------|
| `ID` | ✅ | 纯数字 |
| `名称` | ✅ | `前半段——后半段` 格式 |
| `NSFW` | ✅ | `是` 或 `否` |
| `性行为等级` | 条件 | 0-10 |
| `阶段` | ✅ | 序章/…/后日谈 |
| `第三者` | 条件 | 姓名（身份） |
| `黎恩知情` | 条件 | 描述黎恩角色 |
| `好感影响` | 条件 | `{角色名}: ±{value}` |
| `情境` | ✅ | bullet列表，≥3条 |
| `核心` | ✅ | 设计意图 |
| `章节任务` | ✅ | 1句话核心任务 |
| `章节终止条件` | ✅ | 条件列表 |

> 新增章节后记得更新 `_sex_index.txt`，未确定类型先放"未分类"。

### 2.3 构建命令

```bash
python scripts/build_eldoria.py              # 构建JSON
python scripts/rebuild_all.py                # JSON + 浏览器一键重建
python scripts/generate_chapter_browser.py   # 仅重建浏览器
python scripts/story_tool.py validate        # 验证TXT
python scripts/story_tool.py refs <ID>       # 查引用
python scripts/renumber_events.py            # 全局重编号
```

---

## 三、叙事与写作规则

### 3.1 叙事视角

**摄影机模型**：{{user}}是叙事摄影机——玩家操控的镜头，无实体、无情感、无意志。镜头可指向任何角色（黎恩、Seraphina、劳拉、菲……），对准谁就从谁的感官和内心展开叙事。

- **视角选择流程**：
  1. 玩家视角指令优先（"切换菲娜视角"/"切换黎恩视角"/"双视角"）→立即执行
  2. 无指令时LLM自主判断——根据章节情节和上下文，选最合适的视角
  3. 判断标准：哪个视角让这一刻更好看
- **黎恩视角**：外部世界通过他的感官呈现。表面与内心的张力、矛盾情感、记忆触发、身体叙事
- **Seraphina视角**：与黎恩平等的视角持有者。她的感官、内心、情感锚定——不是例外而是叙事深度的常态
- **视角切换规则**：同一回复内可有限切换，须有明确叙事目的；切换后首句明确当前镜头对准谁；视角持有者不知道的事情不写
- **指代**：始终用"他"或"黎恩"指代黎恩，不用"我"

完整规则详见 `docs/chapter/_写作与视角指令.TXT`。

### 3.2 叙事基调

单线NTRS融合线：**共享时刻 ⇄ 回归时刻**交替推进。
Seraphina永远是强者——共享是双方同意的体验，回归是情感落点。

### 3.3 去AI化写作铁律

> **优秀章节范例**：Ch160/167/336-338/494 — `方案/去机械感和碎片化叙述重要经验.txt`（对话填充五层·标点规范·反碎片化）

| # | 禁令 | 说明 |
|---|------|------|
| 1 | 不写"成本"表述 | 禁用优化/提升/降低/节省/高效/低成本 |
| 2 | 不括号堆砌 | 补充解释直接写进正文 |
| 3 | 不列函数/变量名 | 叙事文本不出现变量名罗列 |
| 4 | 不写空泛结尾 | 禁用"为...支撑""奠定...基础" |
| 5 | 不做未证实声明 | |
| 6 | 不反复"看黎恩确认" | 共享时刻聚焦体验，回归时刻才看黎恩 |
| 7 | 不反复"微笑/平静/掌控" | Seraphina随阶段演进有变化有波动 |
| 8 | 不用"不是…是…"/"没有…只是…" | 禁pivot否定结构。直接写肯定句。详见 `_TEMPLATE_RULES.md` §4.16 |
| 9 | 不写元叙事标记 | 版本变更记git，不入TXT |
| 10 | 不后挂解释 | 修饰融入动词——"轻轻贴上" |
| 11 | 功能性对话可概述 | 保留承载性格或弦外之音的对话 |
| 12 | 不破折号滥用 | 仅三种：标题分隔/对话被打断/插入语。叙事描写和分析字段一律禁止 |
| 13 | 不二字崩 | 角色会正常说话。`"结论。""哪样。"` → `"所以呢？""我不能再那样对你。"` |
| 14 | 问句不用句号 | `？`=真问题/调侃，`。`=陈述落地/冷淡。`"你笑什么？"`不是`。` |
| 15 | 不用短句三连 | 句号结尾≤12字不得连续三句。用逗号串联：`A，B，C。` |
| 16 | 笑了就是笑了 | 不写"胸腔震了一下""喉咙里滚出一声低笑"。直接：他笑了。 |
| 17 | 不碎片对话 | 对话是完整句子。禁"嗯。""嗯。"短句接龙。内心独白同理。详见 `_短对话修复方案.md` |
| 18 | 不金句/人造概念 | 禁角色突然哲学辨析/精巧比喻/自创术语(如"被算进去")。关系是感受不是分类学 |
| 19 | 情绪直接写不绕弯 | 菲娜吃醋就写她吃醋了。动作细节承载情绪，不替代情绪——不让读者猜人物感受 |

> **详细规则**：`docs/story/_TEMPLATE_RULES.md` §4（情境bullet写作规范）§7（去AI化铁律）。**对话修复**：`docs/story/_短对话修复方案.md`（Forms A-G·情感填充五层·对话嵌入动作）。**条件修复**：`docs/story/_条件修复方案.md`（章节任务·终止条件修复规则）。

### 3.4 NSFW规则

- 直接词汇：肉棒/小穴/口交/乳交/插入/射精
- 禁不良气味（体臭/骚味）——足部气味例外
- 禁魔法灯光秀（圣光/鬼之力流转）
- Seraphina永远是强者内核

### 3.5 菲娜人设铁律

**身体先于意志**：菲娜的反应顺序是 身体回应→意识到→做决定。先红耳朵，再决定推开或接受。不从冷开始。

**愤怒来自被无视，不是被触碰**：她说"等一下"不是"停下"。软推拒→对方继续→愤怒。耳朵从红变白是物理标记。

**回归时刻"被接住"原则**：菲娜从第三者处回来后，不需要先演一场对抗（捶胸口/抓领子/推拒）。她需要被接住。黎恩懂她的身体，直接写他给她什么——不需要她先挣扎一下再接受。

**威胁用情感不用物理**：`"再也不理你了"`重于`"不是脚了"`。她在乎才是权力的来源。

**菲娜阶段特征**：娇羞+妩媚+甜美是贯穿全阶段的底色。阶段演进是叠加而非替换。详见：
- `方案/早期章节菲娜微调指南.md`（阶段1-3：娇羞+抗拒+不安 → 娇羞+妩媚+甜美）
- `方案/中期章节菲娜微调指南.md`（阶段4-5：决心+紧张+依赖 → 享受+掌控）
- `方案/阶段五菲娜微调指南.md`（阶段5详规：三色自检·掌控模式·事后分享）
- `方案/阶段六菲娜微调指南.md`（阶段6详规：放纵+设计+极限探索·口交汇报·黎恩忍住）

### 3.6 对话标点速查

| 标点 | 含义 | 示例 |
|------|------|------|
| `。` | 陈述落地、冷淡、克制 | `"我没笑。"` |
| `？` | 真问题、调侃 | `"你笑什么？"` |
| `！` | 情绪上扬：愤怒/羞恼/激动 | `"你明明笑了！"` |
| `~` | 傲娇、拖尾、撒娇 | `"滚啊~"` `"乖~"` |
| `...` | 气息渐弱、犹豫 | `"等...等一下"` |
| `——` | 仅对话被打断 | `"你怎么——"` |

---

## 四、核心设计原则

1. **黎恩永远是黎恩**：灰色骑士内核，`{{user}}`=叙事摄影机
2. **Seraphina永远是Seraphina**：守护者的优雅与孤独
3. **共享 ≠ 背叛**：双方自愿，共享后回归彼此
4. **共享时刻 ⇄ 回归时刻**：两条线交替推进
5. **章节TXT是唯一权威源**：绝不手动编辑JSON
6. **_sex_index.txt是类型权威源**：手工维护，脚本只读

---

## 五、文档依赖链

```
docs/story/{章节}/*.TXT        ← ★ 唯一权威源
docs/story/_TEMPLATE.TXT       ← 章节模板
docs/story/_TEMPLATE_RULES.md  ← 写作规则全集（情境/NSFW/起因/去AI化/黎恩血肉感）
docs/story/_短对话修复方案.md   ← 对话修复方案（Forms A-G·情感填充五层·对话嵌入动作）
docs/story/_条件修复方案.md     ← 终止条件/章节任务修复规则
docs/{chapter,character,world,magic,creature,location,npc}/  ← 设定
方案/*菲娜*微调指南*.md         ← 菲娜阶段特征指南（早期/中期/阶段五/阶段六）
方案/去机械感和碎片化叙述重要经验.txt  ← 对话碎片修复·情感填充·标点规范（Ch491+494精修提炼）
scripts/{build_eldoria,rebuild_all,renumber_events,story_tool,generate_chapter_browser,update_chapter_map}.py
output/Eldoria_V10.12.7.json       ← 派生产物，不可手动编辑
```

---

## 六、角色特征速查

| 角色 | 关键外貌 | 关键行为 |
|------|---------|---------|
| **乔治** | 棕色短发，**不戴眼镜** | 社交腼腆技术自信 |
| **凯尔** | 深蓝短发，戴细框眼镜，异色瞳 | 常说"可以吗"，把性当研究，社交腼腆学术自信 |
| **艾玛** | 李子色长辫，薄荷蓝瞳 | 指尖捻辫梢=思考/紧张，身体永远先出卖她 |
| **黎恩** | **黑发**，青紫瞳，鬼之力刻印 | 磨太刀=冥想，手指穿过她发丝是安抚 |
| **Seraphina** | **粉发**，琥珀瞳，无尖耳 | 走路无声，害羞把脸埋起来，战斗站桩施法 |
| **菲** | 白发，黄绿瞳，150cm | 沉默寡言，行动先于言语，拉黎恩衣角 |
| **亚尔缇娜** | 银白长发及肩，黄绿瞳，142cm | 说话像念报告，黑丝，数据库式思考 |
| **奥蕾莉亚** | 银发，紫瞳，178cm G杯 | 绝对自信，什么都不害羞，温泉狂热 |
| **多尔金** | 矮人，暗棕乱发，148cm | 三字封顶，锻造专注，"不错"=最高评价 |
| **哈根** | 矮人，棕短发，灰蓝眼，142cm | 直率粗放大嗓门，心软，做事时唱矮人古歌 |
| **法林** | 矮人，暗棕长辫，深绿眼，139cm | 憨厚单纯，说"请""谢谢"，指尖微光 |
| **罗恩** | 狼人，绯红眼，183cm(满月205) | 温文尔雅，害羞敲额头，尾巴画弧=舒服 |
| **雷恩** | 深棕短发，深绿瞳，188cm | 话少且慢，磨剑=冥想，从不反问 |

---

---

*版本历史见 git log。开始工作。*
