# 章节TXT三字段新增方案

## 分工

| 字段 | 执行者 | 方式 |
|:--|:--|:--|
| `章节核心目标` | **脚本** | `核心` → `[章节核心目标]`，构建时直接填充，TXT无需单独字段 |
| `章节任务` | **LLM** | 读章节内容 → 写一句话，写入TXT `章节任务:` 字段 |
| `章节终止条件` | **LLM** | 读章节内容 → 写2-4条可验证条件，写入TXT `章节终止条件:` 字段 |

## 理由

`核心` 字段已经是精简的设计意图描述（1-2句话），完全符合俺妹 `[章节核心目标]` 的格式要求——构建脚本直接取 `核心` 值填入 `[章节核心目标]`，无需LLM重写。

`章节任务` 和 `章节终止条件` 是新增信息——前者需要从{{user}}视角提炼一句话任务，后者需要将情境bullet转化为可验证的完成状态——必须由LLM阅读理解后撰写，逐章写入TXT。

**构建脚本 `build_eldoria.py` 已移除对 `章节任务` 和 `章节终止条件` 的自动推导逻辑**——脚本只从TXT读取这两个字段，TXT中有则直接使用，无则输出 `（待填写）`。确保唯一权威源仍是TXT。

## 脚本部分：核心→章节核心目标

构建脚本 `build_eldoria.py` 读取TXT中的 `核心:` 字段，直接填入输出的 `[章节核心目标]:`。**TXT中无需额外添加 `章节核心目标` 字段**——脚本已自动完成映射。

## LLM部分：章节任务 + 章节终止条件

### 参考格式（俺妹第1章）

```
章节任务:
京介必须调查那个突然出现在他家的神秘DVD盒，找出它的主人，并揭开背后隐藏的惊人秘密。

章节终止条件:
京介已确认DVD的主人是桐乃。
桐乃已向京介展示了她隐藏的全部收藏，并说明了她的"妹妹"爱好。
京介最终向桐乃承诺会为她的秘密保密，并答应帮助她解决"烦恼"。
```

### 章节任务写作标准

- 从{{user}}（黎恩）视角
- 一句话，以 `{{user}}必须...` 或 `{{user}}需要...` 开头
- 说清楚本章{{user}}要完成什么
- **不超过30字**

### 章节终止条件写作标准

- 编号列表，用`\n`分隔：`1.条件一\n2.条件二\n3.条件三`
- 每条以角色名开头，描述**可验证的完成状态**
- 2-4条
- 基于情境bullet提炼——每个bullet的核心动作转为一个完成条件

### TXT插入格式

在 `核心:` 字段块后插入：

```
核心: （已有内容）
章节任务: {{user}}必须在陌生的森林中苏醒并了解自己所在之地。
章节终止条件: 1.{{user}}完全苏醒并恢复意识。\n2.Seraphina完成自我介绍。\n3.{{user}}了解Eldoria的基本情况。
```

### 执行方式

按章节顺序分批处理，每批10章：

1. 读取10个章节TXT的完整内容
2. 为每个章节撰写 `章节任务` 和 `章节终止条件`
3. 写入对应TXT（在 `核心:` 字段块后插入）
4. 写完后立即读回验证该批全部文件
5. 输出该批进度：`[01-10/302] 10章完成`

### 防偷懒铁律

| # | 铁律 |
|---|------|
| 1 | 必须逐一读取每个TXT —— 不跳读、不批量推导 |
| 2 | 章节任务每章独一无二 —— 不复制粘贴相似章节 |
| 3 | 章节任务不超过30字 —— 一句完整的话 |
| 4 | 终止条件可验证 —— 每条是具体完成状态，不是模糊目标 |
| 5 | 每批写完后必须读回验证 —— 确认字段已插入 |
